# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
*.egg-info/
dist/
build/

# Node / web
node_modules/
web/.next/
web/out/
.turbo/

# Pipeline data lake (regenerated; potentially large)
pipeline/data/raw/
pipeline/data/staging/
pipeline/data/canonical/

# Published dashboard data (Parquet) IS committed so the GitHub Pages site is
# self-contained (it's open data, reproducible via the pipeline). The large DuckDB
# file and the raw/staging lake stay out (see below).

# Web copy/symlink of data (the CI workflow copies data/ into here; local uses a symlink)
web/public/data

# Env / misc
.env*
!.env.example
.DS_Store

# Playwright MCP artifacts
web/.playwright-mcp/
*.png
.playwright-mcp/

# Generated DuckDB database (regenerable from Parquet)
data/council.duckdb
